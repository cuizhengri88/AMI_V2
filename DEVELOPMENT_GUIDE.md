# 프로젝트 개발 가이드 (DM)

이 문서는 프로젝트의 개발 가이드 및 유지보수를 위한 핵심 원칙과 최근 작업 요청 사항을 정리한 것입니다.

## 1. 프론트엔드 개발 가이드

### 1-1. 유지보수 및 가독성 원칙
- **상세한 한글 주석**: 복잡한 로직이나 상태 관리, 브라우저 기본 동작 제어(`e.preventDefault()`) 등에는 상세한 한글 주석을 작성하여 가독성을 높입니다.
- **다국어 매핑 주석**: `pt('t001')` 또는 `t('key')`와 같은 다국어 함수 호출 시, 소스 코드 내에서 즉시 확인 가능하도록 번역 결과(한글)를 옆에 주석으로 명시합니다.
  - 예: `alert(pt('t001')); // pt('t001') -> 상품 정보가 수정되었습니다.`

### 1-2. 코드 작성 규칙
- **이벤트 제어**: 폼 제출 시 페이지 새로고침을 방지하기 위해 `e.preventDefault()`를 적재적소에 사용하고 그 이유를 주석으로 설명합니다.
- **상태 관리**: `useState`를 사용한 상태 변수 정의 시 각 변수의 역할과 용도를 주석으로 설명합니다.
- **애니메이션**: `framer-motion` 등을 활용한 UI 효과 적용 시 애니메이션의 동작 원리를 기술합니다.

## 2. 최근 작업 요청 사항 반영

### 2-1. 상품 관리 페이지 (ProductManagementPage.tsx)
- **가독성 개선**: 전체 코드에 상세한 한글 주석을 추가함.
*   **다국어 주석 작업**: `pt()` 및 `t()` 함수의 모든 호출 부에 한글 번역 주석을 추가하여 다국어 파일(`ko.json`)을 직접 열어보지 않아도 내용을 알 수 있게 함.
- **기술 주석 보강**: `e.preventDefault()`, `e.target.value`, 모달의 드래그 제어(`useDragControls`) 등에 대한 동작 설명을 추가함.

## 3. 프롬프트 기록 (작업 지침)

이 섹션은 AI 도우미에게 반복적으로 적용할 지침을 기록합니다.

> [!IMPORTANT]
> **개발 시 필수 준수 사항**
> 1. 모든 서비스 로직과 UI 컴포넌트 코드에는 유지보수 측면을 고려하여 상세한 한글 주석을 달 것.
> 2. 다국어 처리 키값(`pt`, `t`) 옆에는 반드시 실제 출력되는 한글 텍스트를 주석으로 표기할 것.
> 3. `e.preventDefault()`와 같은 표준 API 호출에도 그 기능적 의미를 주석으로 설명할 것.
> 4. **타입(Type) 및 인터페이스(Interface)** 정의 시, 각 객체와 하위 속성(Property)들이 무엇을 의미하는지 한글로 상세히 기술할 것. (예: DB 테이블 컬럼 대응 관계 등)
> 5. **데이터 가공 및 변환 로직** (예: `map`, `filter`, `reduce` 등을 사용한 복잡한 연산) 수행 시, 각 단계가 무엇을 계산하는지, 왜 그런 로직이 필요한지 단계별로 한글 주석을 상세히 달 것.

---
*마지막 업데이트: 2026-03-12*
