# 프로젝트 개발 가이드 (DM)

이 문서는 프로젝트의 개발 가이드 및 유지보수를 위한 핵심 원칙과 최근 작업 요청 사항을 정리한 것입니다.

## 1. 프론트엔드 개발 가이드

### 1-1. 유지보수 및 가독성 원칙
- **상세한 한글 주석**: 복잡한 로직이나 상태 관리, 브라우저 기본 동작 제어(`e.preventDefault()`) 등에는 상세한 한글 주석을 작성하여 가독성을 높입니다.
- **다국어 매핑 주석**: `pt('t001')` 또는 `t('key')`와 같은 다국어 함수 호출 시, 소스 코드 내에서 즉시 확인 가능하도록 번역 결과(한글)를 옆에 주석으로 명시합니다.
  - 예: `alert(pt('t001')); // pt('t001') -> 상품 정보가 수정되었습니다.`

### 1-2. 코드 작성 규칙
- **이벤트 제어**: 폼 제출 시 페이지 새로고침을 방지하기 위해 `e.preventDefault()`를 적재적소에 사용하고 그 이유를 주석으로 설명합니다.
- **상태 관리**: `useState`를 사용한 상태 변수 정의 시 각 변수의 역할과 용도를 주석으로 설명합니다.
- **애니메이션**: `framer-motion` 등을 활용한 UI 효과 적용 시 애니메이션의 동작 원리를 기술합니다.

### 1-3. 백엔드 개발 가이드 (Rust/SQL)
- **상세한 로직 설명**: Rust 함수의 기능, 매개변수, 반환값에 대해 상세한 한글 주석을 작성합니다.
- **SQL 쿼리 분석**: `sqlx` 등을 사용하여 작성된 SQL 쿼리 상단에 해당 쿼리의 목적, Join 관계, 필터 조건 등을 한글로 상세히 기술합니다.
- **데이터 구조 매핑**: DB 테이블 컬럼과 대응되는 Rust 구조체(Struct) 필드에 대해 한글 설명을 추가합니다.

## 2. 최근 작업 요청 사항 반영

### 2-1. 공통 코드 관리 (common_code.rs)
- **SQL 상세 주석**: 모든 SELECT, INSERT, UPDATE, DELETE 쿼리에 대해 쿼리 의도와 작동 방식을 한글로 설명함.
- **함수 단위 문서화**: 각 Command 함수의 역할과 비즈니스 로직 흐름을 기술함.

## 3. 프롬프트 기록 (작업 지침)

> [!IMPORTANT]
> **백엔드 개발 시 필수 준수 사항**
> 1. 모든 Rust 함수(`.rs`) 상단에 함수의 역할, 파라미터 의미, 예상 결과를 한글로 작성할 것.
> 2. SQL 쿼리 문자열(`query!`, `query_as!` 등) 바로 위에 해당 SQL이 수행하는 구체적인 작업(예: "특정 그룹의 사용 중인 상세 코드 조회")을 주석으로 달 것.
> 3. 복잡한 SQL의 경우 `WHERE` 절이나 `JOIN` 문이 왜 필요한지 단계별로 설명할 것.
> 4. 에러 처리 로직(`map_err`, `match` 등) 시 에러 발생 원인과 반환되는 메시지의 의미를 한글로 기술할 것.

---
*마지막 업데이트: 2026-03-12*
